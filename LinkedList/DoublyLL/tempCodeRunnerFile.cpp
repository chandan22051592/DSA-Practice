 // // Inserting at head
    // if (position == 1) {
    //     newNode->next = head;
    //     if (head) head->pre = newNode;
    //     head = newNode;
    //     if (!tail) tail = newNode; // If the list was empty
    //     return;
    // }